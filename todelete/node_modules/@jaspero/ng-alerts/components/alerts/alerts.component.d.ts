import { OnInit, ViewContainerRef, OnDestroy, ComponentFactoryResolver } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
import { AlertsService } from '../../services/alerts.service';
import { AlertSettings } from '../../interfaces/alert-settings';
export declare class AlertsComponent implements OnInit, OnDestroy {
    private _service;
    private _resolver;
    private _domSanitize;
    constructor(_service: AlertsService, _resolver: ComponentFactoryResolver, _domSanitize: DomSanitizer);
    compViewContainerRef: ViewContainerRef;
    defaultSettings: AlertSettings;
    settings: AlertSettings;
    private _current;
    private _latestSub;
    private _listener;
    ngOnInit(): void;
    ngOnDestroy(): void;
    private _destroy();
    private _buildItemTemplate(key, value);
}
